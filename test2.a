function test(a, b)
    if a + b == 3
        function test2(c, d)
            return a + b + c + d
        end
        return test2
    else
        return a + b
    end
end

result1 = test(1, 2)
result2 = if result1 is function // is = type check (at least for functions)
    return result1(3, 4)
else
    return result1
end

value = if "true" is string
    return "yes"
else
    return "no"
end

return value